#!/bin/bash

export PROJECT_ID=${project_id}
export REGION=${region}
export RANDOM_SUFFIX=${random_suffix}
export SUBNETWORK_ID=${subnetwork_id}
export DDOS_BUCKET=${ddos_bucket}
export SERVICE_ACCOUNT_EMAIL=${service_account_email}
export DDOS_INCIDENT_TOPIC=${ddos_incidents_topic}
export MODEL_UPLOAD_LOCATION=${model_upload_location}
export MODEL_NAME=${model_name}
export TRAINED_MODEL_NAME=${model_name}-trained
export UPLOADED_MODEL_NAME=${model_name}-uploaded
export ENDPOINT_NAME=${endpoint_name}
export ENDPOINT_ID=${endpoint_id}
